rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Dream images: anyone can read (URL contains a random token), only the Cloud Function
    // (which uses admin credentials and bypasses rules) can write.
    match /dream-images/{userId}/{filename} {
      allow read: if true;
      allow write: if false;
    }
  }
}
